{
    "id": {
        "req_for_initialization": false,
        "display_text": "ID",
        "user_editable": false,
        "validation": {}
    },
    "first_name": {
        "req_for_initialization": true,
        "display_text": "first name",
        "user_editable": true,
        "validation": {
            "validate_as": "name",
            "min_length": 2,
            "max_length": 20,
            "regex": "^\\b[a-zA-Z '.\\-]+\\b$"
        }
    },
    "last_name": {
        "req_for_initialization": true,
        "display_text": "last name",
        "user_editable": true,
        "validation": {
            "validate_as": "name",
            "min_length": 2,
            "max_length": 30,
            "regex": "^\\b[a-zA-Z '.\\-]+\\b$"
        }
    },
    "birth_date": {
        "req_for_initialization": true,
        "display_text": "birth date",
        "user_editable": false,
        "validation": {
            "validate_as": "date",
            "min_length": null,
            "max_length": null,
            "regex": "^\\d{4}(?:-\\d{2}){2}$"
        }
    }
}
