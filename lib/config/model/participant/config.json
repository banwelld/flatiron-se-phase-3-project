{
    "id": {
        "req_for_init": false,
        "display_text": "ID",
        "user_editable": false,
        "validation": {}
    },
    "f_name": {
        "req_for_init": true,
        "display_text": "first name",
        "user_editable": true,
        "validation": {
            "validate_as": "name",
            "min_length": 2,
            "max_length": 20,
            "regex": "^\\b[a-zA-Z '.\\-]+\\b$"
        }
    },
    "l_name": {
        "req_for_init": true,
        "display_text": "last name",
        "user_editable": true,
        "validation": {
            "validate_as": "name",
            "min_length": 2,
            "max_length": 30,
            "regex": "^\\b[a-zA-Z '.\\-]+\\b$"
        }
    },
    "birth_date": {
        "req_for_init": true,
        "display_text": "birth date",
        "user_editable": false,
        "validation": {
            "validate_as": "date",
            "min_length": null,
            "max_length": null,
            "regex": "^\\d{4}(?:-\\d{2}){2}$"
        }
    },
    "team_id": {
        "req_for_init": false,
        "display_text": "team id",
        "user_editable": false,
        "validation": {}
    }
}
