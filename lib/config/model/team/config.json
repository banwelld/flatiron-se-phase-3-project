{
    "id": {
        "display_text": "ID",
        "required": false,
        "user_editable": false,
        "validation": {}
    },
    "name": {
        "display_text": "name",
        "required": true,
        "user_editable": true,
        "validation": {
            "validate_as": "name",
            "min_length": 5,
            "max_length": 30,
            "regex": "[a-zA-Z0-9 '.\\-]"
        }
    },
    "participants": {
        "display_text": "participants",
        "required": false,
        "user_editable": false,
        "validation": {
            "validate_as": "list",
            "max_list_length": 6
        }
    }
}
